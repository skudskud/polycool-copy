{
  "name": "Redis-Bridge",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfile": "apps/subsquid-silo-tests/Dockerfile",
    "context": "apps/subsquid-silo-tests"
  },
  "deploy": {
    "startCommand": "python -m src.redis.bridge",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
