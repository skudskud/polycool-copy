{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd telegram-bot-v2/py-clob-server && python -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "region": "us-east1",
    "healthchecks": {
      "memory": {
        "ram": 512,
        "timeout": 10
      }
    },
    "readinessProbe": {
      "path": "/health",
      "port": "$PORT",
      "initialDelaySeconds": 30,
      "timeoutSeconds": 15,
      "periodSeconds": 10,
      "successThreshold": 1,
      "failureThreshold": 5
    }
  }
}
